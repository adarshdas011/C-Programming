#include<stdio.h>
int main()
    {
	for(int i=1; i<10; i++){
    if(i==5){
	//break;  //( break means Exit the loop now)
	
	//continue; //( continue means skip this interation now)
	          //(5 ko skip karega baki sab print kar dega)
    }
	printf("Adarsh %d \n",i);
    }
    printf("for loop is done");
    return 0;
    }
